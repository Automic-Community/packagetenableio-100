*** Settings ***
Suite Setup       Startup
Suite Teardown    Teardown
Force Tags        launch_scan
Test Template     Launch Scan Template
Library           com.automic.robot.itpa.ItpaLibrary
Library           String
Library           DateTime
Resource          ../../Resources/messages.txt
Resource          ../../Resources/keywords.txt
Resource          ../../Resources/variables.txt
Resource          ../../Resources/actions.txt

*** Test Cases ***    OK_NOK    ScanID
Launch Scan with valid Scan ID
                      [Tags]    main_scenarios
                      OK        ${Scan_ID}

Launch Scan with invalid Scan ID
                      NOK       Invalid

*** Keywords ***
Startup
    Log    *******Start Test "LAUNCH SCAN"*******
    Log    *******Connect to AE*******
    Connect AE

Launch Scan Template
    [Arguments]    ${OK_NOK}    ${ScanID}
    [Documentation]    This test launches a existing scan on Tenable.
    ...    -${OK_NOK}: Boolean value indicate if the action is ENDED_OK or ENDED_NOT_OK. Value of this variable should be OK or NOK.
    ...    -${ScanID}: This field Specifies scan id to launch.
    Generate String
    Init Action    ${_LAUNCH_SCAN_ACTION}
    Nessus Authentication
    Run Keyword If    '${ScanID}' == 'Invalid'    Action Set    &UC4RB_TIO_SCAN_ID#    ${Generated_Name}_${Time}    ELSE    Action Set
    ...    &UC4RB_TIO_SCAN_ID#    ${ScanID}
    Action Execute
    Run Keyword If    '${OK_NOK}' == 'OK'    Assert Success    ELSE    Assert Failure
    Run Keyword If    '${OK_NOK}' == 'OK'    Action Object Variable Should Be Found    &UC4RB_TIO_SCAN_UUID#    ELSE    Action Object Variable Should Not Be Found    &UC4RB_TIO_SCAN_UUID#

Teardown
    Log    *******End Test "LAUNCH SCAN"*******
